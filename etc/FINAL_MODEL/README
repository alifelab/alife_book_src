#
For the parameter sets of  Nsize1, Nsize1  and Nsize3 on page 6.

No. input bias  =1 (constant)
No. input neuron = 7
No. internal neuron = 4
No. of context Neuron =2
No. of output neuron =2

15 +1 Neurons. The connection matrix is given by the colored area in page 6.

multi_davide4_originial.c

# you can evolve a single agent by using a GA flag.
# the evolved NN weights are saved in sasa*
# use the OEKAKI flag to simulate Nant agents on the torus using the evolved connections.
# In case if you want to use sasa4 and sasar, compile it with two weights files.
# In the example below;

seed = atoi(argv[1]);   121433
vehicle_radius =atoi(argv[2]); 5
mutation_rate = atof(argv[3]);  0.3
Nsize3 = atoi(argv[4]); # input + 1bias size 8
Nsize1 = atoi(argv[5]); # up to internal  12
Nsize2 = Nsize-2;

weight matrix = argv[6]; sasa4
wieght matrix 2 = argv[7];  sasar

Nant2 = atoi(argv[8]);  5 #number of ants of the second kind
Vx=atof(argv[9]); 1.1 # max velocity size x
Vy = Vx;          # max velocity size y
heat = atof(argv[10]);  15   #environmental pattern param. 
potential_radius=atof(argv[11]); 4 #environmental pattern param.
Nant = atoi(argv[12]); 20  # number of ants
pheromone = atof(argv[13]); 2 # strength of pheromones
rate_bgp = atof(argv[14]); 0.999 # pheromone evaporation time scale

%alias ccgl='gcc $1 -framework GLUT -framework OpenGL'
%ccgl multi_davide4.c -lm -O3
%./a.out  121433 5 0.3 8 12 sasa4 sasar 5 1.1 15 4 20 2 0.999


